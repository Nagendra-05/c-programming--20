#include <stdio.h>

int main() 
{
    int n,i=2,count;
    printf (" enter the number ");
    scanf("%d",&n);
    while(i<=n/2)
    {
       if (n%i==0)
        count=count+1;
        i++;
    }
    if(count==0)
    printf ("prime");
    else
printf ("not prime");
return 0;
}
